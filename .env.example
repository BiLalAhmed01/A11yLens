# Required for plain-language, AI-generated explanations in the report.
# Get a free key at https://aistudio.google.com/apikey (no credit card needed).
# Without this set, A11yLens still runs and produces a report, but falls
# back to raw axe-core rule text instead of the LLM-authored explanations.
GEMINI_API_KEY=
